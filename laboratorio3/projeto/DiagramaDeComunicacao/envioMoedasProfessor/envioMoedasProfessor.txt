@startuml
skinparam linestyle ortho
actor "Professor" as professor
rectangle "<u> professorRoute" as route
rectangle "<u> professorController" as controller
rectangle "<u> professorModel" as model

professor -right-> route: 1.1 POST/enviar-moedas/:cpf 
route -right-> controller: 1.2 enviarMoedas(req, res) 
controller -right-> model: 1.3 enviarMoedas(professor_id, aluno_id, quantidade, motivo)
model -left-> controller: 1.4 {message: "Moedas transferidas com sucesso.", saldoProfessor: professor.saldo_moedas - quantidade, saldoAluno: aluno.saldo_moedas + quantidade, transacao_id: result.insertId, professor, aluno };
controller -left-> route:  1.5 res.status(200).json(data)
@enduml